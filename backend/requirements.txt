# ═══════════════════════════════════════════════════════════════
# requirements.txt — Moodify backend
# Configurado para CPU + GPU Intel Iris Xe (sin CUDA, sin bitsandbytes)
# ═══════════════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ── Utilidades ────────────────────────────────────────────────
python-dotenv==1.0.1
PyJWT==2.8.0

# ── Supabase + dependencias internas EXACTAS ──────────────────
supabase
gotrue==2.9.3
httpx==0.27.2
postgrest==0.17.1

# ── LLM — CPU + GPU Intel (sin CUDA, sin bitsandbytes) ────────
# torch CPU: funciona con Intel Iris Xe vía memoria compartida
torch==2.2.2
torchvision==0.17.2

# transformers y accelerate: reparto automático de capas CPU/GPU
transformers==4.40.2
accelerate==0.29.3

# sentencepiece y protobuf: requeridos por Gemma tokenizer
sentencepiece==0.2.0
protobuf==4.25.3

# ── OPCIONAL: Intel Extension for PyTorch ─────────────────────
# Descomenta si quieres usar XPU (Intel Arc/Iris Xe) directamente.
# Requiere instalar manualmente: pip install intel-extension-for-pytorch
# intel-extension-for-pytorch

# ── NO incluidos (requieren NVIDIA/CUDA) ──────────────────────
# bitsandbytes  ← SOLO NVIDIA, no compatible con Intel
# flash-attn    ← SOLO NVIDIA